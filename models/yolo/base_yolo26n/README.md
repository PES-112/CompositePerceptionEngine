# Base YOLO26n

Local registry entry for the pretrained base `yolo26n.pt` checkpoint used to initialize CPE fine-tuning and retained-class comparisons.

Binary weights are tracked in Git at:

```text
models/yolo/base_yolo26n/yolo26n.pt
```

Use this path instead of keeping `yolo26n.pt` in the repository root.
