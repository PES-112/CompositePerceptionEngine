# CPE YOLO26n Hazards v3 From Base

Preferred CPE detector registry entry for the v3-from-base YOLO26n hazard checkpoint.

Source checkpoint:

```text
training/runs/cpe_yolo26n_hazards_v3_from_base/weights/best.pt
```

Export artifacts are written under:

```text
models/yolo/cpe_yolo26n_hazards_v3_from_base/exports/
```

Commit binary model artifacts such as `.pt`, `.onnx`, `.engine`, `.xml`, `.bin`, or `.torchscript` files when they are crucial to the CPE end product, along with the manifests and notes.
